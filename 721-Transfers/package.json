{
  "name": "nft_minted",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ betothor/nft_minted",
    "create-local": "graph create --node http://localhost:8020/ betothor/nft_minted",
    "remove-local": "graph remove --node http://localhost:8020/ betothor/nft_minted",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 betothor/nft_minted"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.46.1",
    "@graphprotocol/graph-ts": "^0.29.3"
  }
}
